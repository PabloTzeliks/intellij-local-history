/**
 * Pacote: model
 *
 * Data classes imutáveis usadas para transferir dados entre camadas.
 * Não contêm lógica de negócio — são apenas "containers" de dados.
 *
 * - SnapshotRequest: dados capturados no momento do save (antes de processar)
 * - SnapshotEntry: representa um snapshot já salvo no disco
 */
package com.github.pablotzeliks.intellijlocalhistory.model
